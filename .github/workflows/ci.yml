name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - name: Install root dependencies
        run: npm ci
      - name: TypeScript type check
        run: npx tsc --noEmit
      - name: Run tests (excludes enterprise/bench — moved to mg-enterprise)
        run: npx vitest run --exclude='**/{enterprise,bench}/**'

  build:
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build distribution
        run: ./build.sh
      - name: Verify distribution archive
        run: test -f dist/miniature-guacamole.tar.gz

  dashboard:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: dashboard/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Generate Prisma client (if schema exists)
        run: if [ -d "prisma" ]; then npx prisma generate; fi
      - name: TypeScript type check
        run: npx tsc --noEmit
      - name: Run tests
        run: npm test
